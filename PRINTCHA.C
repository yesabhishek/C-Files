/* Printing n characters from starting pos of a string*/
#include<stdio.h>
#include<conio.h>
main()
{ char str[10];int pos,totc,i;
clrscr(); flushall();printf("\n ENTER A STRING:");
scanf("%s",&str); flushall();
printf("Enter Starting Position:"); scanf("%d",&pos);
flushall();
printf("Enter Total Number of characters to be printed:"); 
scanf("%d",&totc);flushall();
if((totc+pos-1) > strlen(str)) { return;}
for(i=pos-1;i<(totc+pos-1);i++) { printf("%c",str[i]);}
}


























































































































































